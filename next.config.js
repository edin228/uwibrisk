/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains:['925681.smushcdn.com', 'us-west-2.graphassets.com']
  },
  reactStrictMode: true,
}

module.exports = nextConfig
